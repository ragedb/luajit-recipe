# luajit-recipe
LuaJIT Conan Recipe for RageDB

This recipe is compatible with Conan 2.x.

## Usage

To build, package, and test the library locally, run:

```bash
conan create . --version 2.1.0-beta3-2023-01-04
```